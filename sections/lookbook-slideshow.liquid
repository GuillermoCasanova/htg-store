{{ 'vendor-swiper.css' | asset_url | stylesheet_tag }}
{{ 'lookbook-slideshow.css' | asset_url | stylesheet_tag }}
<script type="module" src="{{ 'lookbook-slideshow.js' | asset_url }}" defer="defer"></script>

<lookbook-slideshow> 
  <div class="lookbook-slideshow" data-lookbook-images-slideshow>
      <ul  class="lookbook-slideshow__inner" data-lookbook-images-slideshow-wrapper>
      {% for block in section.blocks %}
          <li class="lookbook-slideshow__slide" data-lookbook-images-slideshow-slide>
              <div class="lookbook-slideshow__slide__inner">
              <img
                  srcset="{%- if block.settings.image.width >= 165 -%}{{ block.settings.image | image_url: width: 165 }} 165w,{%- endif -%}
                  {%- if block.settings.image.width >= 360 -%}{{ block.settings.image | image_url: width: 360 }} 360w,{%- endif -%}
                  {%- if block.settings.image.width >= 533 -%}{{ block.settings.image | image_url: width: 533 }} 533w,{%- endif -%}
                  {%- if block.settings.image.width >= 720 -%}{{ block.settings.image | image_url: width: 720 }} 720w,{%- endif -%}
                  {%- if block.settings.image.width >= 940 -%}{{ block.settings.image | image_url: width: 940 }} 940w,{%- endif -%}
                  {%- if block.settings.image.width >= 1066 -%}{{ block.settings.image | image_url: width: 1066 }} 1066w,{%- endif -%}
                  {%- if block.settings.image.width >= 1600 -%}{{ block.settings.image | image_url: width: 1600 }} 1600w,{%- endif -%}
                  {{ block.settings.image | image_url }} {{ block.settings.image.width }}w"
                  src="{{ block.settings.image | image_url: width: 533 }}"
                  sizes="(min-width: 700px) 50vw, 100vw"
                  alt="{{ block.settings.image.alt | escape }}"
                  loading="lazy"
                  width="{{ block.settings.image.width }}"
                  height="{{ block.settings.image.height }}">
              </div>
          </li>
      {% endfor %}
      </ul>
      <div  data-lookbook-images-slideshow-pagination class="lookbook-slideshow__pagination">
      </div>
  </div>
</lookbook-slideshow>


<div><div id="image-mapper-5637" class="image-mapper" has-placeholder data-mapper-module="mapper" data-mapper='{ "url": "https://easy-image-mapper.herokuapp.com/api/v2/mappers/5637" }'><img src="https://cdn.shopify.com/s/files/1/0013/1111/3328/t/26/assets/htg-ss22_-_concrete_jungle_lookbook3.webp?v=1655922893" class="im-tmp" style="display:block;max-width:100%;"></div></div>

{% schema %}
{
  "name": "Lookbook Slideshow",
  "tag": "section",
  "class": "lookbook-slideshow-container",
  "templates": [
    "page"
  ],
  "settings": [
    {
      "type": "text",
      "id": "name",
      "label": "Collection Name"
    }
  ],
  "blocks": [
    {
      "type": "slideshow_slide",
      "name": "Slide",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Lookbook Slideshow"
    }
  ]
}
{% endschema %}